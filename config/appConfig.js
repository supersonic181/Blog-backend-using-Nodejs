const serverConfig = {
    port: "8000",
    hostname: "localhost"
}

const dbConfig = {
    host: 'localhost',
    user: 'root',
    password: '12345',
    database: 'atorvis_blog'
}

module.exports = {
    serverConfig,
    dbConfig
}